services:
  - type: web
    name: waste-ml-service
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/ML/Detector.py
    envVars:
      - key: PORT
        value: 3000
    plan: free